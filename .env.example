# Copy to .env for local development. NEVER commit real values.
# All secrets and provider keys must live server-side in the Phase 1 architecture.

# Example (leave empty in this prototype):
FIREBASE_API_KEY=
SPORTS_DATA_PROVIDER_KEY=
